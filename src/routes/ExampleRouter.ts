/*
*
*   @Importa o router principal
    import RouterMain from "./Router.js";

    @Importa a controller dessa rota
    import ExampleController from "../controllers/ClientesController.js";
*
*
*
*
*   export default class ExampleRouter extends RouterMain {
*        constructor() {
*          @Define a controller pra rota
*         super(new ExampleController());
*       }
*
*       protected setupRoutes(): void {
*          @Seta as rotas 
*         this.router.get('/', this.controller.index);
*     }
*   }
*
*
*/